package com.ged.workflow.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class WorkflowTaskAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertWorkflowTaskAllPropertiesEquals(WorkflowTask expected, WorkflowTask actual) {
        assertWorkflowTaskAutoGeneratedPropertiesEquals(expected, actual);
        assertWorkflowTaskAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertWorkflowTaskAllUpdatablePropertiesEquals(WorkflowTask expected, WorkflowTask actual) {
        assertWorkflowTaskUpdatableFieldsEquals(expected, actual);
        assertWorkflowTaskUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertWorkflowTaskAutoGeneratedPropertiesEquals(WorkflowTask expected, WorkflowTask actual) {
        assertThat(actual)
            .as("Verify WorkflowTask auto generated properties")
            .satisfies(a -> assertThat(a.getId()).as("check id").isEqualTo(expected.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertWorkflowTaskUpdatableFieldsEquals(WorkflowTask expected, WorkflowTask actual) {
        assertThat(actual)
            .as("Verify WorkflowTask relevant properties")
            .satisfies(a -> assertThat(a.getAssigneeId()).as("check assigneeId").isEqualTo(expected.getAssigneeId()))
            .satisfies(a -> assertThat(a.getStatus()).as("check status").isEqualTo(expected.getStatus()))
            .satisfies(a -> assertThat(a.getAction()).as("check action").isEqualTo(expected.getAction()))
            .satisfies(a -> assertThat(a.getComment()).as("check comment").isEqualTo(expected.getComment()))
            .satisfies(a -> assertThat(a.getAssignedDate()).as("check assignedDate").isEqualTo(expected.getAssignedDate()))
            .satisfies(a -> assertThat(a.getDueDate()).as("check dueDate").isEqualTo(expected.getDueDate()))
            .satisfies(a -> assertThat(a.getCompletedDate()).as("check completedDate").isEqualTo(expected.getCompletedDate()))
            .satisfies(a -> assertThat(a.getReminderSent()).as("check reminderSent").isEqualTo(expected.getReminderSent()))
            .satisfies(a -> assertThat(a.getDelegatedTo()).as("check delegatedTo").isEqualTo(expected.getDelegatedTo()))
            .satisfies(a -> assertThat(a.getDelegatedDate()).as("check delegatedDate").isEqualTo(expected.getDelegatedDate()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertWorkflowTaskUpdatableRelationshipsEquals(WorkflowTask expected, WorkflowTask actual) {
        assertThat(actual)
            .as("Verify WorkflowTask relationships")
            .satisfies(a -> assertThat(a.getInstance()).as("check instance").isEqualTo(expected.getInstance()))
            .satisfies(a -> assertThat(a.getStep()).as("check step").isEqualTo(expected.getStep()));
    }
}
