package fr.smartprod.paperdms.document.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class MetaBookmarkAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMetaBookmarkAllPropertiesEquals(MetaBookmark expected, MetaBookmark actual) {
        assertMetaBookmarkAutoGeneratedPropertiesEquals(expected, actual);
        assertMetaBookmarkAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMetaBookmarkAllUpdatablePropertiesEquals(MetaBookmark expected, MetaBookmark actual) {
        assertMetaBookmarkUpdatableFieldsEquals(expected, actual);
        assertMetaBookmarkUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMetaBookmarkAutoGeneratedPropertiesEquals(MetaBookmark expected, MetaBookmark actual) {
        assertThat(actual)
            .as("Verify MetaBookmark auto generated properties")
            .satisfies(a -> assertThat(a.getId()).as("check id").isEqualTo(expected.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMetaBookmarkUpdatableFieldsEquals(MetaBookmark expected, MetaBookmark actual) {
        assertThat(actual)
            .as("Verify MetaBookmark relevant properties")
            .satisfies(a -> assertThat(a.getUserId()).as("check userId").isEqualTo(expected.getUserId()))
            .satisfies(a -> assertThat(a.getEntityType()).as("check entityType").isEqualTo(expected.getEntityType()))
            .satisfies(a -> assertThat(a.getEntityName()).as("check entityName").isEqualTo(expected.getEntityName()))
            .satisfies(a -> assertThat(a.getCreatedDate()).as("check createdDate").isEqualTo(expected.getCreatedDate()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMetaBookmarkUpdatableRelationshipsEquals(MetaBookmark expected, MetaBookmark actual) {
        // empty method
    }
}
