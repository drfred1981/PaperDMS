package fr.smartprod.paperdms.monitoring.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class SystemHealthAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertSystemHealthAllPropertiesEquals(SystemHealth expected, SystemHealth actual) {
        assertSystemHealthAutoGeneratedPropertiesEquals(expected, actual);
        assertSystemHealthAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertSystemHealthAllUpdatablePropertiesEquals(SystemHealth expected, SystemHealth actual) {
        assertSystemHealthUpdatableFieldsEquals(expected, actual);
        assertSystemHealthUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertSystemHealthAutoGeneratedPropertiesEquals(SystemHealth expected, SystemHealth actual) {
        assertThat(actual)
            .as("Verify SystemHealth auto generated properties")
            .satisfies(a -> assertThat(a.getId()).as("check id").isEqualTo(expected.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertSystemHealthUpdatableFieldsEquals(SystemHealth expected, SystemHealth actual) {
        assertThat(actual)
            .as("Verify SystemHealth relevant properties")
            .satisfies(a -> assertThat(a.getServiceName()).as("check serviceName").isEqualTo(expected.getServiceName()))
            .satisfies(a -> assertThat(a.getStatus()).as("check status").isEqualTo(expected.getStatus()))
            .satisfies(a -> assertThat(a.getVersion()).as("check version").isEqualTo(expected.getVersion()))
            .satisfies(a -> assertThat(a.getUptime()).as("check uptime").isEqualTo(expected.getUptime()))
            .satisfies(a -> assertThat(a.getCpuUsage()).as("check cpuUsage").isEqualTo(expected.getCpuUsage()))
            .satisfies(a -> assertThat(a.getMemoryUsage()).as("check memoryUsage").isEqualTo(expected.getMemoryUsage()))
            .satisfies(a -> assertThat(a.getErrorRate()).as("check errorRate").isEqualTo(expected.getErrorRate()))
            .satisfies(a -> assertThat(a.getLastCheck()).as("check lastCheck").isEqualTo(expected.getLastCheck()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertSystemHealthUpdatableRelationshipsEquals(SystemHealth expected, SystemHealth actual) {
        // empty method
    }
}
