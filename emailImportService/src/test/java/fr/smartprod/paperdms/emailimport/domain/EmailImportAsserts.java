package fr.smartprod.paperdms.emailimport.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class EmailImportAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertEmailImportAllPropertiesEquals(EmailImport expected, EmailImport actual) {
        assertEmailImportAutoGeneratedPropertiesEquals(expected, actual);
        assertEmailImportAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertEmailImportAllUpdatablePropertiesEquals(EmailImport expected, EmailImport actual) {
        assertEmailImportUpdatableFieldsEquals(expected, actual);
        assertEmailImportUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertEmailImportAutoGeneratedPropertiesEquals(EmailImport expected, EmailImport actual) {
        assertThat(actual)
            .as("Verify EmailImport auto generated properties")
            .satisfies(a -> assertThat(a.getId()).as("check id").isEqualTo(expected.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertEmailImportUpdatableFieldsEquals(EmailImport expected, EmailImport actual) {
        assertThat(actual)
            .as("Verify EmailImport relevant properties")
            .satisfies(a -> assertThat(a.getFromEmail()).as("check fromEmail").isEqualTo(expected.getFromEmail()))
            .satisfies(a -> assertThat(a.getToEmail()).as("check toEmail").isEqualTo(expected.getToEmail()))
            .satisfies(a -> assertThat(a.getSubject()).as("check subject").isEqualTo(expected.getSubject()))
            .satisfies(a -> assertThat(a.getBody()).as("check body").isEqualTo(expected.getBody()))
            .satisfies(a -> assertThat(a.getBodyHtml()).as("check bodyHtml").isEqualTo(expected.getBodyHtml()))
            .satisfies(a -> assertThat(a.getReceivedDate()).as("check receivedDate").isEqualTo(expected.getReceivedDate()))
            .satisfies(a -> assertThat(a.getProcessedDate()).as("check processedDate").isEqualTo(expected.getProcessedDate()))
            .satisfies(a -> assertThat(a.getStatus()).as("check status").isEqualTo(expected.getStatus()))
            .satisfies(a -> assertThat(a.getFolderId()).as("check folderId").isEqualTo(expected.getFolderId()))
            .satisfies(a -> assertThat(a.getDocumentTypeId()).as("check documentTypeId").isEqualTo(expected.getDocumentTypeId()))
            .satisfies(a -> assertThat(a.getAttachmentCount()).as("check attachmentCount").isEqualTo(expected.getAttachmentCount()))
            .satisfies(a -> assertThat(a.getDocumentsCreated()).as("check documentsCreated").isEqualTo(expected.getDocumentsCreated()))
            .satisfies(a -> assertThat(a.getAppliedRuleId()).as("check appliedRuleId").isEqualTo(expected.getAppliedRuleId()))
            .satisfies(a -> assertThat(a.getErrorMessage()).as("check errorMessage").isEqualTo(expected.getErrorMessage()))
            .satisfies(a -> assertThat(a.getMetadata()).as("check metadata").isEqualTo(expected.getMetadata()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertEmailImportUpdatableRelationshipsEquals(EmailImport expected, EmailImport actual) {
        assertThat(actual)
            .as("Verify EmailImport relationships")
            .satisfies(a -> assertThat(a.getAppliedRule()).as("check appliedRule").isEqualTo(expected.getAppliedRule()));
    }
}
