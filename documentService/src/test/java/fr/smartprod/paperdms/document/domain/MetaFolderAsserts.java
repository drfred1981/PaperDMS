package fr.smartprod.paperdms.document.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class MetaFolderAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMetaFolderAllPropertiesEquals(MetaFolder expected, MetaFolder actual) {
        assertMetaFolderAutoGeneratedPropertiesEquals(expected, actual);
        assertMetaFolderAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMetaFolderAllUpdatablePropertiesEquals(MetaFolder expected, MetaFolder actual) {
        assertMetaFolderUpdatableFieldsEquals(expected, actual);
        assertMetaFolderUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMetaFolderAutoGeneratedPropertiesEquals(MetaFolder expected, MetaFolder actual) {
        assertThat(actual)
            .as("Verify MetaFolder auto generated properties")
            .satisfies(a -> assertThat(a.getId()).as("check id").isEqualTo(expected.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMetaFolderUpdatableFieldsEquals(MetaFolder expected, MetaFolder actual) {
        assertThat(actual)
            .as("Verify MetaFolder relevant properties")
            .satisfies(a -> assertThat(a.getName()).as("check name").isEqualTo(expected.getName()))
            .satisfies(a -> assertThat(a.getDescription()).as("check description").isEqualTo(expected.getDescription()))
            .satisfies(a -> assertThat(a.getPath()).as("check path").isEqualTo(expected.getPath()))
            .satisfies(a -> assertThat(a.getIsShared()).as("check isShared").isEqualTo(expected.getIsShared()))
            .satisfies(a -> assertThat(a.getCreatedDate()).as("check createdDate").isEqualTo(expected.getCreatedDate()))
            .satisfies(a -> assertThat(a.getCreatedBy()).as("check createdBy").isEqualTo(expected.getCreatedBy()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMetaFolderUpdatableRelationshipsEquals(MetaFolder expected, MetaFolder actual) {
        assertThat(actual)
            .as("Verify MetaFolder relationships")
            .satisfies(a -> assertThat(a.getParent()).as("check parent").isEqualTo(expected.getParent()));
    }
}
