package fr.smartprod.paperdms.scan.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class ScannerConfigurationAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertScannerConfigurationAllPropertiesEquals(ScannerConfiguration expected, ScannerConfiguration actual) {
        assertScannerConfigurationAutoGeneratedPropertiesEquals(expected, actual);
        assertScannerConfigurationAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertScannerConfigurationAllUpdatablePropertiesEquals(ScannerConfiguration expected, ScannerConfiguration actual) {
        assertScannerConfigurationUpdatableFieldsEquals(expected, actual);
        assertScannerConfigurationUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertScannerConfigurationAutoGeneratedPropertiesEquals(ScannerConfiguration expected, ScannerConfiguration actual) {
        assertThat(actual)
            .as("Verify ScannerConfiguration auto generated properties")
            .satisfies(a -> assertThat(a.getId()).as("check id").isEqualTo(expected.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertScannerConfigurationUpdatableFieldsEquals(ScannerConfiguration expected, ScannerConfiguration actual) {
        assertThat(actual)
            .as("Verify ScannerConfiguration relevant properties")
            .satisfies(a -> assertThat(a.getName()).as("check name").isEqualTo(expected.getName()))
            .satisfies(a -> assertThat(a.getScannerType()).as("check scannerType").isEqualTo(expected.getScannerType()))
            .satisfies(a -> assertThat(a.getIpAddress()).as("check ipAddress").isEqualTo(expected.getIpAddress()))
            .satisfies(a -> assertThat(a.getPort()).as("check port").isEqualTo(expected.getPort()))
            .satisfies(a -> assertThat(a.getProtocol()).as("check protocol").isEqualTo(expected.getProtocol()))
            .satisfies(a -> assertThat(a.getManufacturer()).as("check manufacturer").isEqualTo(expected.getManufacturer()))
            .satisfies(a -> assertThat(a.getModel()).as("check model").isEqualTo(expected.getModel()))
            .satisfies(a -> assertThat(a.getDefaultColorMode()).as("check defaultColorMode").isEqualTo(expected.getDefaultColorMode()))
            .satisfies(a -> assertThat(a.getDefaultResolution()).as("check defaultResolution").isEqualTo(expected.getDefaultResolution()))
            .satisfies(a -> assertThat(a.getDefaultFormat()).as("check defaultFormat").isEqualTo(expected.getDefaultFormat()))
            .satisfies(a -> assertThat(a.getCapabilities()).as("check capabilities").isEqualTo(expected.getCapabilities()))
            .satisfies(a -> assertThat(a.getIsActive()).as("check isActive").isEqualTo(expected.getIsActive()))
            .satisfies(a -> assertThat(a.getCreatedDate()).as("check createdDate").isEqualTo(expected.getCreatedDate()))
            .satisfies(a -> assertThat(a.getLastModifiedDate()).as("check lastModifiedDate").isEqualTo(expected.getLastModifiedDate()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertScannerConfigurationUpdatableRelationshipsEquals(ScannerConfiguration expected, ScannerConfiguration actual) {
        // empty method
    }
}
