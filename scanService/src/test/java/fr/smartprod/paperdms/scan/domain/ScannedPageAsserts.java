package fr.smartprod.paperdms.scan.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class ScannedPageAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertScannedPageAllPropertiesEquals(ScannedPage expected, ScannedPage actual) {
        assertScannedPageAutoGeneratedPropertiesEquals(expected, actual);
        assertScannedPageAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertScannedPageAllUpdatablePropertiesEquals(ScannedPage expected, ScannedPage actual) {
        assertScannedPageUpdatableFieldsEquals(expected, actual);
        assertScannedPageUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertScannedPageAutoGeneratedPropertiesEquals(ScannedPage expected, ScannedPage actual) {
        assertThat(actual)
            .as("Verify ScannedPage auto generated properties")
            .satisfies(a -> assertThat(a.getId()).as("check id").isEqualTo(expected.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertScannedPageUpdatableFieldsEquals(ScannedPage expected, ScannedPage actual) {
        assertThat(actual)
            .as("Verify ScannedPage relevant properties")
            .satisfies(a -> assertThat(a.getScanJobId()).as("check scanJobId").isEqualTo(expected.getScanJobId()))
            .satisfies(a -> assertThat(a.getPageNumber()).as("check pageNumber").isEqualTo(expected.getPageNumber()))
            .satisfies(a -> assertThat(a.getSha256()).as("check sha256").isEqualTo(expected.getSha256()))
            .satisfies(a -> assertThat(a.gets3Key()).as("check s3Key").isEqualTo(expected.gets3Key()))
            .satisfies(a -> assertThat(a.gets3PreviewKey()).as("check s3PreviewKey").isEqualTo(expected.gets3PreviewKey()))
            .satisfies(a -> assertThat(a.getFileSize()).as("check fileSize").isEqualTo(expected.getFileSize()))
            .satisfies(a -> assertThat(a.getWidth()).as("check width").isEqualTo(expected.getWidth()))
            .satisfies(a -> assertThat(a.getHeight()).as("check height").isEqualTo(expected.getHeight()))
            .satisfies(a -> assertThat(a.getDpi()).as("check dpi").isEqualTo(expected.getDpi()))
            .satisfies(a -> assertThat(a.getDocumentId()).as("check documentId").isEqualTo(expected.getDocumentId()))
            .satisfies(a -> assertThat(a.getScannedDate()).as("check scannedDate").isEqualTo(expected.getScannedDate()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertScannedPageUpdatableRelationshipsEquals(ScannedPage expected, ScannedPage actual) {
        assertThat(actual)
            .as("Verify ScannedPage relationships")
            .satisfies(a -> assertThat(a.getScanJob()).as("check scanJob").isEqualTo(expected.getScanJob()));
    }
}
